import React from 'react';
import Container from '../../../components/container';
import Sidebar from '../../../components/profile/sidebar';

const Favourite = () => {
 return (
     <Container
         sidebar={
             <Sidebar/>
         }
     >
         <div>Favourite must be here</div>

     </Container>
 );
};

export default Favourite;
